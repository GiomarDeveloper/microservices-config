spring:
  data:
    mongodb:
      uri: mongodb://mongodb:27017/db_credit

external:
  services:
    customer:
      url: http://host.docker.internal:8081

logging:
  level:
    com.bank.credit: DEBUG